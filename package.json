{
  "name": "front",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "tsopenapi": "npx openapi-typescript openapi.yml --output src/types/generated-schema.ts"
  },
  "dependencies": {
    "clsx": "^1.1.1",
    "preact": "^10.5.13"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.0.0",
    "prettier": "^2.2.1",
    "sass": "^1.32.12",
    "typescript": "^4.1.3",
    "vite": "^2.2.3"
  }
}
